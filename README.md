Orchestration: Kubernetes
================================

This library allows you to deploy Docker containers to Kubernetes clusters.

Installation
--------------

Install this library as a development dependency (for use in your Gulpfile) with:

```
npm install --save-dev orchestration-kubernetes
```